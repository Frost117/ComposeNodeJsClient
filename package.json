{
  "name": "compose-nodejs",
  "version": "1.0.0",
  "description": "Fetches data from an endpoint and imports it to another",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node --env-file=.env dist/index.js",
    "build:start": "tsc && node --env-file=.env dist/index.js",
    "test": "node --test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@clack/prompts": "^1.0.1"
  }
}
